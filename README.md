# Brandon Y. Chang Portfolio

This folder is ready for GitHub Pages at `https://brandonychang.github.io`.

## Publish it

1. Sign in to GitHub and create a public repository named exactly `brandonychang.github.io`.
2. Open the repository and choose **Add file**, then **Upload files**.
3. Extract the ZIP, then drag every extracted file and the `assets` folder into GitHub's upload area. Do not upload the ZIP itself. Keep the HTML files beside `index.html`, `styles.css`, and `script.js`. Keep `chatdb-interface.png` inside the `assets` folder.
4. Choose **Commit changes**.
5. Open **Settings**, then **Pages**.
6. Under **Build and deployment**, choose **Deploy from a branch**.
7. Select the `main` branch and `/ (root)`, then choose **Save**.
8. After GitHub finishes publishing, visit `https://brandonychang.github.io`.

Keep the HTML, CSS, JavaScript, and README files in the repository's top level. Keep all screenshots inside `assets`. Google hosts the Figtree, Newsreader, and Aboreto fonts used by the design.
